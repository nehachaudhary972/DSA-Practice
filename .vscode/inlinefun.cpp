#include<bits/stdc++.h>
using namespace std;
inline int max(int x,int y)
{  
    if(x>y)
    return x;
    else
    return y;
}

int main()
{
    cout<<"greatest of 4 and 5 is: "<<max(4,5);
    return 0;
}